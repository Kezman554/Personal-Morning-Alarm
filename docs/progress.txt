2026-05-03  Initial Android project scaffold
  - Created module layout: data/, ui/, service/, receiver/, challenge/, util/
  - Build setup: AGP 8.7.3, Kotlin 1.9.25, Gradle 8.9, KSP for Room
  - compileSdk 36, targetSdk 34, minSdk 26 (Galaxy A32 5G is API 30)
  - Dependencies: Room, CameraX, ML Kit barcode, Material3, Navigation, Lifecycle/ViewModel
  - Manifest permissions: NFC, CAMERA, FLASHLIGHT, VIBRATE, WAKE_LOCK,
    SCHEDULE_EXACT_ALARM, USE_EXACT_ALARM, RECEIVE_BOOT_COMPLETED,
    POST_NOTIFICATIONS, FOREGROUND_SERVICE (+ media playback / special use)
  - Placeholder MainActivity shows app name via view binding
  - Built debug APK successfully (~29 MB; over PRD's 20 MB target -
    revisit by switching ML Kit barcode to Play Services unbundled variant)
  - Git initialised, origin set to Kezman554/Personal-Morning-Alarm (not pushed)
  - Install on device deferred: no device attached over ADB during scaffold

2026-06-03  Build fix: compileSdk 36 to 35 (commit 8dadebc, pushed)
  - app/build.gradle: compileSdk 36 became compileSdk 35
  - Why: AGP 8.7.3 only supports compiling up to SDK 35; targeting 36 was
    asking it to build against a platform it doesn't understand, choking the sync
  - targetSdk (34) and minSdk (26) unchanged

2026-06-03  Navigation + ViewBinding scaffold
  - Three screens wired up: HomeFragment, StatisticsFragment, SettingsFragment
    (ui/), each with a placeholder layout and the nullable _binding ViewBinding
    pattern (cleared in onDestroyView)
  - res/navigation/nav_graph.xml: 3 destinations, start = Home
  - MainActivity now hosts a NavHostFragment and calls
    bottomNav.setupWithNavController(); back-button/up handled by NavController
  - activity_main.xml: FragmentContainerView + Material BottomNavigationView
  - res/menu/bottom_nav_menu.xml: menu item IDs match nav destination IDs so
    NavigationUI auto-wires tab switching (no manual click listeners)
  - Added vector icons ic_home, ic_statistics, ic_settings; nav title strings
  - ViewBinding was already enabled in build.gradle (no change needed)
  - Built debug APK successfully; not yet run on device (none attached)
  - APK still ~28.5 MB (ML Kit barcode bundle); size trim still outstanding

2026-06-03  Room database layer
  - 5 entities (data/entity/): AlarmConfig, AlarmEvent, NfcTag, ContentToggle,
    BundledQuote. AlarmConfig.alarmTime is minutes-since-midnight; AlarmEvent.date
    is "yyyy-MM-dd" (indexed). Unique indexes on NfcTag.tagId and
    ContentToggle.contentType. NfcTag.order maps to column tag_order (SQL keyword)
  - Enums MorningGoal, ContentType (data/model/) + Converters for Room
  - DAO per entity (data/dao/) with CRUD plus getActiveNfcTags,
    getEnabledContentToggles, getSuccessRateSince, getSuccessDates(Desc/Asc)
  - AppDatabase: all entities/DAOs, @TypeConverters, thread-safe getInstance()
  - AlarmRepository: suspend wrappers over every DAO + stats:
    getWeeklySuccessRate (SQL, trailing 7 days), getCurrentStreak/getLongestStreak
    computed in Kotlin via LocalDate (window/recursive-CTE unreliable at minSdk 26)
  - "Success" for streaks = stage2Success == true; current streak anchors on
    today or yesterday so an unattempted morning doesn't break a live streak
  - ui/ViewModelFactory: builds any ViewModel(AlarmRepository)
  - MainActivity has a temporary Room smoke test (insert + read AlarmConfig,
    logs under tag PMA); marked TODO, kept in for now. Builds successfully
  - First on-device run confirmed on Galaxy A32 5G (serial RFCT31JE4AZ): smoke
    test round-tripped AlarmConfig (inserted id=1, read back alarmTime=390 /
    06:30, EXERCISE). Note: test inserts a throwaway config row on each launch
    until removed

2026-06-03  Alarm scheduling system
  - util/AlarmScheduler: scheduleAlarm/cancelAlarm/isAlarmScheduled using
    setExactAndAllowWhileIdle + RTC_WAKEUP (fires through Doze). Default
    requestCode 1001; PendingIntent uses FLAG_IMMUTABLE
  - API 31+ exact-alarm handling: canScheduleExactAlarms() check +
    exactAlarmSettingsIntent() to ACTION_REQUEST_SCHEDULE_EXACT_ALARM
  - receiver/AlarmReceiver: logs when fired (tag PMA); registered in manifest
    (exported=false); action com.personalmorningalarm.action.ALARM_FIRED
  - HomeFragment: temporary "Schedule test alarm (1 min)" button (TODO); routes
    to permission settings if exact alarms not allowed
  - DEVICE NOTE: primary phone (SM-A326B / Galaxy A32 5G) is now on Android 13
    (API 33), not API 30 as CLAUDE.md/PRD state. USE_EXACT_ALARM is auto-granted
    on 33 so no permission prompt was needed in testing
  - END-TO-END VERIFIED on device: tapped button 19:17:16 -> "Alarm scheduled
    for ...696" -> receiver logged "Alarm fired at 2026-06-03 19:18:16" (exactly
    on the 60s mark)

2026-06-03  Alarm service + dismissal activity
  - service/AlarmService: foreground service (type mediaPlayback), HIGH-importance
    notification channel "alarm_channel" (sound/vibration handled by service, not
    channel; bypasses DND). Looping MediaPlayer on default TYPE_ALARM ringtone
    with USAGE_ALARM attrs; repeating waveform vibration. start()/stop() helpers,
    isRunning flag
  - ui/AlarmDismissalActivity: setShowWhenLocked/setTurnScreenOn + requestDismiss
    Keyguard (window FLAG_* fallback < API 27). Back button swallowed via
    OnBackPressedCallback. Temporary "Stop alarm" button (deviation from "empty"
    so the looping alarm can be stopped without force-stop; marked TODO)
  - AlarmReceiver now starts AlarmService (exact-alarm FGS exemption)
  - Activity launched via full-screen-intent notification (reliable bg activity
    start on API 29+), not raw startActivity
  - Manifest: registered service + activity (showOnLockScreen, turnScreenOn,
    singleTask, excludeFromRecents); added USE_FULL_SCREEN_INTENT
  - NOTE: on API 33 the FSI needs POST_NOTIFICATIONS granted; granted via adb for
    testing. In-app runtime request still TODO
  - VERIFIED on device (driven via adb, screen asleep/Dozing): alarm fired ->
    AlarmService foreground (isForeground=true, id 2001) -> screen Dozing->Awake
    -> AlarmDismissalActivity became focused window over lock screen -> on-screen
    Stop button logged "AlarmService stopping" and the service record cleared.
  - Human-confirmed by Nick: screen slept, then the alarm sounded WITH vibration

2026-06-03  Boot persistence
  - receiver/BootReceiver: on BOOT_COMPLETED, reads current AlarmConfig via
    AlarmRepository (goAsync + IO coroutine) and, if isEnabled, reschedules
    through AlarmScheduler. Logs outcome under tag PMA
  - util/AlarmScheduler: added scheduleDailyAlarm(alarmTimeMinutes) +
    nextTriggerMillis() — computes next occurrence of a daily wall-clock time
    (today if still future, else tomorrow) via java.time. Reused later by the
    real "save alarm" screen
  - Manifest: registered BootReceiver (exported=true, intent-filter
    BOOT_COMPLETED). RECEIVE_BOOT_COMPLETED permission already present
  - VERIFIED on device via adb reboot: restart cleared all alarms; on boot
    BootReceiver logged "rescheduled alarm (alarmTime=390, ok=true)" and
    dumpsys alarm showed the pending alarm at 2026-06-04 06:30:00 (next
    occurrence of the saved 06:30 config). Note: Samsung staggers boot
    broadcasts — ours arrived ~4 min after sys.boot_completed=1

2026-06-03  Shake challenge (Stage 1) + dismissal wiring
  - challenge/ChallengeInterface: start()/stop() + onProgress(Float 0..1)/
    onComplete callbacks (settable lambdas). Common contract for all challenges
  - challenge/ShakeChallenge: TYPE_ACCELEROMETER via SensorManager; "shaking" =
    delta of acceleration magnitude > 15.0f (gravity cancels in the delta, so
    orientation-independent). Accrues cumulative active-shake time toward 15s
    target with a 300ms pause tolerance; auto-stops + fires onComplete at 100%.
    threshold + target are constructor params (defaulted)
  - ui/AlarmDismissalActivity: wired ShakeChallenge in — progress bar + percent,
    starts in onResume / stops in onPause. onComplete stops AlarmService +
    finishes. Replaced temp Stop button with shake-to-dismiss but KEPT a fallback
    Stop button (TODO) while tuning
  - VERIFIED on device (full alarm path, Nick shaking): 0% -> 25 -> 50 -> 75 ->
    100% -> "Shake challenge complete" -> AlarmService stopped. Bar filled, alarm
    stopped. Feedback: needed a VERY hard shake (sat at 0% ~45s before catching);
    15.0f threshold is stiff. Keeping as-is for now; future Settings option to
    tune shake vigour planned (maps to ShakeChallenge shakeThreshold arg)

2026-06-03  Stage 1 full UI + Stage 2 handoff (+ shake tuning)
  - AlarmDismissalActivity: progress bar, seconds-remaining, red->green
    background (ArgbEvaluator), haptic pulses while actively shaking. On
    complete: stops AlarmService, logs Stage 1 success to DB, brief success
    message, then transitions to Stage 2 (blue) and starts the countdown
  - service/CountdownService: foreground (specialUse), runs Stage 2 timer from
    config stage2DurationMinutes, exposes remainingSeconds StateFlow + ticking
    notification. Nuclear-on-expiry still TODO. Registered in manifest
  - Stage 1 success persisted via AlarmRepository (upsert today's AlarmEvent)
  - SHAKE TUNING (3 on-device rounds with Nick): threshold 15->8->4.0f; 15 and 8
    impractically hard (~11% of vigorous shaking counted at 8). Fixed a real bug:
    progress reset when switching arms — rotation was recreating the activity and
    onResume re-zeroed progress. Now orientation-locked (nosensor), KEEP_SCREEN_ON,
    and start() preserves accumulated progress; pause tolerance 300->500ms
  - VERIFIED on device: shake filled 0->100% (no reset), alarm stopped, "Stage 1
    success logged", CountdownService started (600s), blue Stage 2 countdown shown
  - KNOWN follow-up (Nick deferred): detection favours fast twitches over big
    swings (delta-of-samples = frequency detector). Switch to accel-magnitude
    deviation from gravity so forceful swings register too; bundle with the
    planned sensitivity setting

2026-06-04  CountdownService brought up to full spec
  - Was created with the Stage 2 work; this round added the missing pieces:
    * Duration falls back to AlarmConfig.stage2DurationMinutes (default 10 min)
      when no EXTRA_DURATION_SECONDS is supplied (reads config off the main
      thread); start() duration arg is now optional
    * Broadcasts ACTION_COUNTDOWN_EXPIRED on expiry (future nuclear-alarm hook;
      nuclear still TODO)
    * Notification tap opens MainActivity (contentIntent)
  - Temporary dev "Test countdown (15s)" button on HomeFragment (TODO)
  - VERIFIED on device (driven via adb, no shaking): tapped button ->
    "CountdownService starting (15 s, explicit)" -> notification ticked down
    0:12 -> 0:08 -> 0:05 -> at exactly 15s "countdown expired — broadcasting
    COUNTDOWN_EXPIRED" -> service stopped. Config-read fallback is code-complete
    (logs "from config"); only the explicit-duration path was device-tested

2026-06-04  NFC tag registration in Settings
  - SettingsFragment rebuilt: NFC section with status text, "Register New Tag"
    button, active-tag count, RecyclerView list (label + location + delete)
  - MVVM: SettingsViewModel(repository) via existing ViewModelFactory; reactive
    Room Flow queries (NfcTagDao.observeAll / observeActiveCount) so the list and
    count auto-refresh. NfcTagAdapter (ListAdapter + DiffUtil)
  - Registration uses NfcAdapter.enableReaderMode (modern equivalent of
    foreground dispatch; cleaner with fragments — no MainActivity.onNewIntent).
    Reader callback (binder thread) -> UI thread -> label/location dialog ->
    viewModel.registerTag (dedupe on tagId, order = current count)
  - NFC availability handled: no adapter -> "no NFC" + button disabled; disabled
    -> "NFC off, tap to open settings" + opens ACTION_NFC_SETTINGS
  - Added recyclerview dependency (was only transitive via Material)
  - VERIFIED on device: settings UI renders; NFC-off status message confirmed via
    adb toggle; Nick registered a real tag ("Living Room tissue box") -> saved,
    shown in list, "Active tags: 1", persisted across navigation (DB-backed)

2026-06-04  Stage 2 NFC checkpoint sequence
  - challenge/NfcCheckpointManager: shuffles active tags at construction (random
    order each morning), tracks current index, validates scanned tagId ->
    CheckpointResult (Correct/Wrong/SequenceComplete/AlreadyComplete)
  - AlarmDismissalActivity Stage 2: shows "X of N" + "Go to: <label>" + the
    CountdownService timer. NFC via enableForegroundDispatch + onNewIntent (clean
    in an activity, unlike the fragment's reader mode). Correct -> advance; wrong
    -> "Wrong tag - find <label>" + buzz; final -> stop countdown, log Stage 2
    success (with seconds), show "All done", finish. No tags registered (or NFC
    off) -> "tap to confirm" fallback button
  - VERIFIED on device (Nick, 2 real tags "...tissue box" + "crocodile"): shake
    cleared Stage 1; Stage 2 randomised 2 checkpoints; logs show "Checkpoint
    correct -> next: crocodile", "Wrong tag - still need: crocodile", "All
    checkpoints tapped", "Stage 2 success logged (85 s)". All mechanics confirmed
  - GOTCHA FOUND: NFC taps are NOT delivered while the secure lock screen is up
    (taps did nothing until Nick unlocked). Added promptUnlockIfNeeded(): on
    Stage 2 start when locked, calls requestDismissKeyguard + shows "Unlock your
    phone to tap the tags" hint.

2026-06-04  Stage 2 hardening (3 follow-up fixes from on-device testing)
  - Session guard: a re-fired alarm during an active wake session restarted
    Stage 1 (a stray +60s test alarm fired mid-Stage-2 and hijacked it). Added
    AlarmDismissalActivity.isSessionActive; AlarmReceiver ignores alarms while a
    dismissal session is on screen. VERIFIED (no second alarm on retest)
  - Unlock hint VERIFIED on device (prompt appeared when Stage 2 started locked)
  - NFC reader mode: switched Stage 2 from enableForegroundDispatch to
    enableReaderMode. Foreground dispatch only delivered the FIRST tag on this
    Samsung (system NFC UI stole focus, killed dispatch); consecutive taps were
    dropped. Reader mode (as used in Settings) fixed it. VERIFIED: wrong tag ->
    correct tag -> advance -> complete, all consecutive reads worked
  - Lingering-chip popup: after completion, disabling NFC immediately let the
    system grab a still-present chip (Samsung "new tag" popup). Now reader mode
    stays engaged (taps ignored once stage=DONE) until the activity pauses. Minor
    fix, NOT yet device-verified

2026-06-05  Delete-tag confirmation
  - SettingsFragment: tapping a tag's delete (trash) now shows a "Delete tag? /
    Remove <label> from your checkpoints?" dialog before removing (was instant).
    Cancel keeps it, Delete calls viewModel.deleteTag
  - VERIFIED on device (adb-driven): dialog shows the tag label; Cancel left
    "Active tags: 2" unchanged

2026-06-05  Content screens between NFC checkpoints
  - challenge/ContentScreenManager: maps each gap between checkpoints to an
    enabled content type, cycling in display order (N tags -> N-1 gaps)
  - AlarmDismissalActivity: after a correct tap (not the last), shows an inline
    content panel before the next checkpoint. Quote (random BundledQuote +
    author), Stretch (suggestions + 5-min countdown, auto-advances or Continue),
    Placeholder (shows morning goal). Taps ignored while content is up
  - PersonalMorningAlarmApp (new Application): seeds 40 bundled quotes + default
    content toggles (quote+stretch on, placeholder off) on first launch
  - Settings: content-toggle switches (Quote/Stretch/Placeholder) backed by a
    reactive ContentToggle Flow; SettingsViewModel.setContentEnabled
  - VERIFIED on device: seeding logged "Seeded 40 bundled quotes" + default
    toggles; toggle switches render in Settings. NOT yet verified: the actual
    content-between-taps during a Stage 2 run (needs a shake-through with 3+ tags)
  - Added temporary registration logging in SettingsFragment while diagnosing a
    tag-add hiccup (was a timing issue — tag tapped before reader was listening)
  - STRETCH duration hardcoded 5 min (TODO: make configurable in settings)

2026-06-05  Split NFC tag management to its own screen (Nick's suggestion)
  - Settings was getting cramped (tag list + content toggles). Moved NFC tag
    management to a dedicated NfcTagsFragment (+ NfcTagsViewModel,
    fragment_nfc_tags.xml). Settings now shows a "Manage NFC tags" button +
    the content toggles only
  - nav_graph: added nfcTagsFragment destination + action_settings_to_nfcTags;
    SettingsFragment navigates to it. Back returns to Settings
  - SettingsViewModel trimmed to content toggles; tag CRUD lives in NfcTagsViewModel
  - VERIFIED on device: Settings shows Manage button + 3 toggles (no list); tapping
    opens the tags page ("Active tags: 3" — Nick's 3rd tag); Back returns to Settings

2026-06-06  Shake detection rewrite + crash fix + content screens verified
  - ROOT CAUSE of recurring shake pain: delta-between-samples detection is
    rate-sensitive and aliases against the vibration motor — under-registered real
    shakes. Rewrote ShakeChallenge to FORCE-based: |accel magnitude - gravity| >
    threshold (3.5 m/s^2), independent of sample rate and orientation
  - CRASH fix: had switched to SENSOR_DELAY_FASTEST, which needs
    HIGH_SAMPLING_RATE_SENSORS on API 31+ and threw SecurityException ->
    AlarmDismissalActivity crashed on resume (this, not a vibration self-trigger,
    was the "alarm turned itself off" on 06-05 AND 06-06). Reverted to
    SENSOR_DELAY_GAME (~50Hz, no permission)
  - Removed Stage 1 vibration from AlarmService: force-based detection would read
    the motor as shaking; sound-only also keeps Stage 1 gentle (PRD). Also removed
    the activity's shake haptic pulses (same feedback risk)
  - Notification tap mid-shake reset progress: AlarmService full-screen intent used
    FLAG_ACTIVITY_CLEAR_TASK, recreating the activity. Removed it; singleTask now
    reuses the instance. Notification text -> "Shake your phone to dismiss"
  - VERIFIED on device (Nick): shake felt right ("small deliberate effort"); diag
    showed ~50Hz, 0.0 m/s^2 at rest (no false trigger), 53-103 m/s^2 while shaking
    (threshold 3.5 has big headroom). Quote + Stretch content screens both appeared.
    Full Stage 1 -> content -> Stage 2 flow works end to end
  - Diagnostics logging removed after tuning

2026-06-06  Content toggle settings + stretch duration selector
  - Most of this was already built (Morning Content switches + seeding). New work:
  - Renamed Settings section "Content screens" -> "Morning Content"
  - Stretch duration selector (5/10 min radio) under the Stretch switch, shown
    only when stretching is enabled. Saved to ContentToggle.durationMinutes
  - Schema change: added duration_minutes to content_toggles via a NON-destructive
    Room migration (v1->v2, ALTER TABLE ADD COLUMN default 5) so registered NFC
    tags + seeded quotes are preserved. Bumped DB version to 2 + addMigrations
  - SettingsViewModel.setStretchDuration; AlarmDismissalActivity reads the STRETCH
    toggle's durationMinutes for the stretch screen (was hardcoded 5)
  - Display order: fixed for V1 (display_order seeded 0/1/2; no drag-reorder)
  - VERIFIED on device (adb): "Morning Content" + 5/10 selector render; tapping
    10 min persists across navigation; migration kept Active tags: 3 (no wipe)
  - NOT re-tested: full Stage 2 run confirming a disabled toggle drops its content
    + the 10-min stretch taking effect (wired via getEnabledContentToggles; needs
    a shake-through)

2026-06-06  Pause Stage 2 countdown during content screens (Nick's catch)
  - Bug Nick spotted: the Stage 2 countdown (default 10 min) kept running during
    the stretch screen (up to 10 min), so a long stretch would expire the timer
    and (once built) trigger the nuclear alarm mid-stretch — through no fault.
  - CountdownService: added pause()/resume() (ACTION_PAUSE/RESUME). pause cancels
    the timer keeping remaining seconds; resume restarts from there.
  - AlarmDismissalActivity: pause on showContentScreen, resume on onContentContinue.
    So the deadline only counts time spent finding/tapping checkpoints, not content.
  - VERIFIED on device (Nick): stretch shown -> "Countdown paused at 579s";
    after ~2 min on the stretch screen -> "Countdown resumed at 579s" (frozen,
    9:40 as expected). Quote pause confirmed too (584s -> 584s)

2026-06-06  Stage 2 success flow (streak + success screen)
  - onStage2Complete now: stops CountdownService, clears all app notifications
    (NotificationManager.cancelAll), records the AlarmEvent (stage1+stage2 success
    + time), computes current streak, and shows a success screen: "Great job!" +
    "Day X of your streak!" + a morning-goal reminder. Auto-dismiss after 5s or tap
  - Streak via repository.getCurrentStreak() after the event is written
  - VERIFIED on device: notifications cleared (Nick confirmed); log showed "Stage 2
    success logged (36 s)" + "streak now 1" (correct — 06-05 was all aborts, so
    today is a fresh Day 1). BUT Nick didn't see the on-screen stat — likely a
    stray tap dismissed the success screen instantly (whole screen was tap-to-
    dismiss). FIX: tap-to-dismiss now only activates after a 1.5s grace period so
    the screen stays readable. Needs a re-confirm next run
  - RE-CONFIRMED on device after grace fix: Nick saw the success screen with the
    streak line ("Day X of your streak!"). Stage 2 success flow complete

2026-06-07  Nuclear alarm infrastructure (Stage 2 failure)
  - service/NuclearAlarmService: foreground (mediaPlayback), HIGH-importance
    DND-bypass channel "nuclear_channel". Looping MediaPlayer on the default
    RINGTONE tone (deliberately DISTINCT from Stage 1, which uses the ALARM tone)
    via USAGE_ALARM; strong repeating vibration waveform (600/400ms, max amplitude,
    VibrationAttributes.USAGE_ALARM on API 33 to bypass DND); flashlight strobe via
    CameraManager.setTorchMode toggled every 250ms. Ongoing full-screen-intent
    notification -> NuclearDismissalActivity
  - VOLUME SAFEGUARD: FORCE_MAX_VOLUME const defaults FALSE, so loudness follows the
    device alarm slider (lets us test without waking the house — Nick's chosen
    safeguard). Flip to true for production to pin STREAM_ALARM to max. STROBE_ENABLED
    const (true; strobe is silent so safe during testing)
  - ui/NuclearDismissalActivity: lock-screen flags (showWhenLocked/turnScreenOn/
    dismissKeyguard), back swallowed (OnBackPressedCallback + onKeyDown belt-and-
    suspenders), temp "Stop nuclear" button (gauntlet replaces it next task)
  - WIRING: CountdownService.onFinish now starts NuclearAlarmService directly (still
    broadcasts ACTION_COUNTDOWN_EXPIRED too); AlarmDismissalActivity registers a
    receiver for that broadcast and finish()es so Stage 2 doesn't linger behind the
    nuclear screen; AlarmReceiver ignores re-fired alarms while NuclearAlarmService
    .isRunning. Manifest: service (mediaPlayback) + activity (singleTask, nosensor,
    showOnLockScreen); nuclear strings added
  - VERIFIED on device (Nick, via Test countdown 15s): nuclear fired on expiry —
    loud, strobe, vibrate all confirmed. Alarm correctly KEPT RUNNING through back/
    home (sound is service-driven, can't be silenced by leaving). Notification
    brought the screen back; temp Stop dismissed
  - KNOWN LIMITATION (accepted/deferred by Nick): can't make the window itself
    inescapable without a SYSTEM_ALERT_WINDOW overlay — stock Android can't block
    HOME, and onUserLeaveHint relaunch is blocked by background-activity-launch
    rules. Deferred: the real guarantee (alarm never goes silent until dismissed)
    holds, which is what matters. Revisit overlay only if needed
  - Back-button hardening (onKeyDown) added this session but NOT yet re-verified on
    device. OUT OF SCOPE this task (next): the dismissal gauntlet (30s shake + math
    + NFC + typed phrase) and failure logging / streak reset on nuclear

2026-06-07  Nuclear dismissal gauntlet (4 challenges) + failure logging
  - NuclearDismissalActivity rebuilt: shows all 4 challenges at once, each with a
    ○ -> ✓ status, completable in ANY order; alarm only silences once all 4 done:
    * Shake: reuses ShakeChallenge with targetDurationMs=30s
    * challenge/MathChallenge: random two-digit + or - (subtraction kept non-negative),
      isCorrect() verifier
    * challenge/NfcChallenge: tap ANY registered tag (reader mode). Auto-completes if
      no tags registered or NFC off, so the user can't get trapped
    * challenge/TypingChallenge: phrase "I am awake and getting up", case- and
      whitespace-insensitive
  - Failure logged on ENTRY (reaching the screen IS the failure): records today's
    AlarmEvent with nuclearTriggered=true, stage2Success=false, so it stands even if
    the user quits. Streak reset: AlarmRepository.getCurrentStreak now returns 0 when
    today's event has nuclearTriggered=true (nuclearTriggered column already existed
    in the v2 schema — no migration needed)
  - CORRECTED A FALSE LESSON (Nick's catch): the earlier belief that the vibration
    motor self-triggers the force-based shake was an UNVERIFIED precaution bundled
    into the 06-06 crash fix; the real self-dismiss cause was the SENSOR_DELAY_FASTEST
    SecurityException crash. So nuclear now VIBRATES throughout the gauntlet (no
    pause); briefly added a vibration-pause workaround then removed it. Memory updated
  - VERIFIED on device (Nick, "all good"): nuclear fired on countdown expiry, all 4
    challenges completed in arbitrary order, alarm silenced only after the 4th ✓,
    streak reset. Shake bar did NOT self-fill from the vibration (false lesson
    confirmed dead). Layout: scrollable cards on black, white text, green ✓ (themes.xml
    NuclearCard/Status/Input styles)
  - Temp "Stop nuclear" button removed (gauntlet replaces it)

2026-06-07  Home screen (real alarm config + stats)
  - HomeFragment rebuilt from the placeholder: big tap-to-edit time
    (TimePickerDialog, respects device 12/24h), enable/disable SwitchMaterial,
    morning-goal radio (Exercise/Project), status line ("Alarm set for 6:30 AM" /
    "Alarm off"), current streak, and "This week: X/Y days"
  - HomeViewModel: config + stats as reactive Flows off Room. config from new
    AlarmConfigDao.observeCurrent(); stats recomputed on AlarmEventDao.observeAll()
    emissions (current streak + weekly success/attempted day counts). Edits persist
    to the SINGLE config row (update-in-place, or create one disabled if none) so
    rows don't accumulate
  - Repository/DAO: observeCurrentConfig, observeEvents, getWeeklySuccessCounts
    (success vs attempted distinct days over trailing 7); DAO countSuccessDaysSince
    / countAttemptedDaysSince
  - Scheduling stays in the fragment (has Context + permission flow): enable ->
    scheduleDailyAlarm, disable -> cancelAlarm, time change while enabled ->
    reschedule. Missing exact-alarm permission reverts the switch + opens settings
  - REMOVED the temporary Room smoke test from MainActivity — it inserted a fresh
    AlarmConfig row every launch, which would clobber the user's saved alarm now
    that real config UI exists. (Old installs may have leftover config rows; getCurrent
    uses the latest, and the first toggle/edit reconciles it)
  - Two dev test buttons (test alarm / countdown) kept under a "Dev tools" label
  - Built + installed on device; Nick approved the commit (no specific on-device
    stat-match issues reported)

2026-06-08  Configurable NFC checkpoint sequence length
  - AlarmConfig: new sequenceLength field (default 5) = how many NFC taps Stage 2
    requires. Non-destructive Room migration v2->v3 (ALTER TABLE alarm_config ADD
    COLUMN sequenceLength DEFAULT 5); DB version bumped, MIGRATION_2_3 registered
  - NfcCheckpointManager(tags, sequenceLength): builds the morning's order from the
    length, not just the pool. length <= #tags -> N unique tags (shuffled, no
    repeats); length > #tags -> repeats to fill, never the same tag twice in a row
    (only unavoidable with a single registered tag, which then just repeats). total
    now reflects the sequence length, so Stage 2's "X of N" updates for free
  - Settings: new "Number of NFC taps" NumberPicker under the NFC section. Primary
    range 1..registered-tags; a previously-saved value above the current tag count
    (e.g. after deleting tags) stays selectable rather than being silently dropped,
    and the hint warns when taps will repeat. Disabled with a "register a tag" hint
    when none exist. SettingsViewModel: sequenceLength + activeTagCount StateFlows
    (combined for the picker) + setSequenceLength (single-row update-in-place)
  - AlarmDismissalActivity reads config.sequenceLength and passes it to the manager
  - Unit tests added (app/src/test, first in the project): NfcCheckpointManagerTest
    covers unique-pick, exact-fit, repeat-with-no-adjacent-dupes (200 randomised
    iterations), single-tag fallback, empty pool, wrong-tag-no-advance. All pass
  - VERIFIED: assembleDebug clean; testDebugUnitTest green; installed on SM-A326B;
    app launched with no Room migration crash (home reads alarm_config on start, so
    the v3 schema validated against the upgraded v2 DB). On-device end-to-end (set
    length 3 with 5 tags -> 3 taps; then fewer tags -> repeats) still to be run by Nick

2026-06-08  Content-screen fixes (follow-ups from on-device testing)
  - Picker ceiling bug: the "Number of NFC taps" max was maxOf(tagCount, savedValue),
    so scrolling DOWN to <= tagCount collapsed the ceiling and you couldn't go back
    up. Fixed to a fixed ceiling maxOf(tagCount, 10) independent of the current value
  - Content cycling bug (Nick's catch): with more taps than enabled content types the
    gaps cycled the types again (a 2nd quote, 2nd stretch). Morning content should be
    once each per morning. ContentScreenManager.contentForGap now getOrNull(gapIndex)
    instead of [gapIndex % size] — each enabled type shows once in order, later gaps
    get nothing
  - Tests: added ContentScreenManagerTest (once-each-then-null, empty, hasContent).
    testDebugUnitTest green; reinstalled on SM-A326B

2026-06-09  Daily alarm re-arm fix (alarm fired once then never again)
  - BUG (Nick hit it for real): alarm went off the morning after he set the time,
    but not the next day. Root cause: setExactAndAllowWhileIdle is a ONE-SHOT alarm
    and AlarmReceiver only started AlarmService — nothing re-armed the next day. It
    appeared to work because saving the time scheduled that one occurrence, and
    reboots re-arm via BootReceiver; a normal day left no alarm. dumpsys confirmed:
    last fire 06-08 07:00, no future alarm pending at all
  - FIX: AlarmReceiver.rescheduleNextDay() — after firing (even when the fire is
    ignored due to an active session/nuclear), reads the config and, if enabled,
    schedules the next occurrence (nextTriggerMillis from now+1min so today's just-
    fired time rolls to tomorrow). Mirrors the proven BootReceiver pattern; starts
    AlarmService first so the brief FGS-start allowance isn't spent on the DB read
  - VERIFIED on device (silent: alarm stream forced to min during test): fired a
    test alarm -> logcat "Alarm fired ... starting AlarmService" + "Re-armed next
    alarm (alarmTime=420, ok=true)" + dumpsys showed a NEW pending ALARM_FIRED at
    2026-06-10 07:00:00. Alarm now self-perpetuates daily (BootReceiver still covers
    reboots). Left enabled + armed for tomorrow 07:00; restored alarm volume
  - NOTE: force-stopping the app cancels its pending AlarmManager alarms (Android
    stopped-state) — re-arm needs a fire, a reboot, or re-saving the alarm. Real
    proof is firing on consecutive mornings (Nick to confirm)

2026-06-09  Quote screen 15s dismiss lock
  - Quote content screen now locks its Continue button for 15s: button disabled +
    greyed (alpha 0.4), a "15s..1s" countdown shows just above it (new
    tv_continue_countdown), and on finish Continue re-enables with a brief scale-pop
    cue. Makes the user actually sit with the quote
  - Duration is a constant CONTENT_DISMISS_LOCK_MS (15_000) so future fixed-duration
    content screens can reuse startContentDismissLock(); setContinueEnabled(enabled)
    handles the grey-out. showContentScreen resets the button to enabled + cancels
    any prior lock/animation each time, so only the quote screen locks
  - Stretch screen unchanged — it keeps its own configurable duration + auto-advance
    and never calls the lock. contentDismissTimer cancelled in onContentContinue and
    onDestroy alongside stretchTimer
  - Built + installed on SM-A326B. On-device check (shake -> Stage 2 -> quote gap ->
    Continue disabled 15s then enables) needs a physical run — Nick to confirm

2026-06-10  Statistics screen (streaks, weekly chart, heatmap, history)
  - StatisticsFragment was a placeholder; built the full screen. StatisticsViewModel
    derives a StatisticsUiState off repository.observeEvents() (same reactive pattern
    as HomeViewModel): currentStreak + longestStreak (existing repo helpers), four
    trailing 7-day WeekBar buckets, 28-day DayCell grid, and the full event history.
    Multiple events on one date collapse to the latest (by timestamp) for the grid +
    week buckets; history shows every event
  - CHART CHOICE: custom Canvas views, NOT MPAndroidChart. The APK is already ~28 MB
    (over PRD's 20 MB; ML Kit), and MPAndroidChart needs the JitPack repo added to
    settings.gradle (currently FAIL_ON_PROJECT_REPOS, google+mavenCentral only). The
    two charts here are trivial, so a dependency + repo change wasn't worth it
    * WeeklyBarChartView: one bar per week, height = success fraction, "S/A" or "—"
      label on top, week-start date below. Faint track shows empty weeks
    * DayHeatmapView: 7-col rounded-square grid, green/red/grey by DayOutcome; square
      cells, height derived from width in onMeasure
    * Both resolve colorPrimary/colorOnSurface via theme (adapt to dark/light) +
      stat_success/stat_failure/stat_empty colors (stat_empty is translucent grey so
      it reads on both themes)
  - EventHistoryAdapter (ListAdapter + DiffUtil, like NfcTagAdapter): date "EEE d MMM",
    ✓/✗ glyph (green/red), goal + duration ("1:25" >=60s else "45s") or "missed".
    item_event_history.xml. RecyclerView nested in the ScrollView with
    nestedScrollingEnabled=false
  - VERIFIED on device (SM-A326B, real DB — 6 events 06-03..06-10): current/longest
    streak 1/1; bars "— / — / 0/1 / 4/5" (4 Jun filled ~80%); heatmap greens for
    04/06/08/10 Jun, red for 07 (nuclear) + 03 Jun, grey gaps; history list dates/
    goals/durations/glyphs all correct. testDebugUnitTest green; no regressions

2026-06-10  Alarm PIN (guards alarm time, on/off, Stage 2 limit)
  - Optional 4-digit PIN so you can't change wake-up settings half-asleep. util/
    PinManager: SharedPreferences ("pma_security"), stores SHA-256(per-install random
    salt + pin) — never the raw digits. isPinSet/setPin/verify(constant-time)/clearPin.
    A 4-digit PIN is low-entropy by nature; this is a nuisance guard, not real security
  - ui/PinDialog: custom keypad dialog (dialog_pin.xml) — own on-screen 0-9 pad (system
    IME never shown), filled/empty dots (pin_dot_filled/empty drawables), Cancel +
    backspace. Doesn't decide meaning: on the 4th digit it calls onComplete(pin, dialog)
    so the caller can verify / advance / showError / dismiss. PinKey button style added
  - ui/PinPrompts: the four flows over that one keypad — guard (run action now if no
    PIN, else prompt+verify), setup (enter twice to confirm), change (verify current ->
    setup), disable (verify current -> clear)
  - GATED (req 4): HomeFragment alarm-time tap + enable/disable switch; SettingsFragment
    Stage 2 time-limit row. Switch flips visually on tap, so when gated it's reverted
    immediately and only re-applied on a correct PIN (wrong/cancel leaves it untouched)
  - NOT gated (req 5): content toggles, stretch duration, NFC tag registration, NFC tap
    count. (Sound/theme/quote mgmt don't exist as controls yet)
  - Settings "Alarm PIN" section: status + Set (no PIN) / Change + Disable (PIN set),
    visibility via renderPinSection()
  - NEW Stage 2 control: req 4 named "Stage 2 timer duration" but no UI existed (only
    AlarmConfig.stage2DurationMinutes). Added a tappable "Stage 2 time limit" row ->
    NumberPicker dialog (5-15 min, PRD range) + SettingsViewModel.stage2Duration flow /
    setStage2Duration. Gated behind the PIN
  - VERIFIED on device (SM-A326B, adb/uiautomator-driven): set PIN 1234 (two-step
    confirm) -> "PIN is on"; alarm-time tap prompted; wrong 9999 -> "Wrong PIN - try
    again" (dots reset, picker stayed shut); correct 1234 -> TimePicker opened; enable
    switch + Stage 2 row both prompted (switch unchanged after cancel); Stage 2 picker
    opened on correct PIN; disable 1234 -> "No PIN set", controls open freely again.
    Build + testDebugUnitTest green; device left in clean no-PIN state

2026-06-10  Quote pool management UI
  - New QuoteManagementFragment (Settings -> "Manage quotes" button): RecyclerView
    of all quotes with optional italic "— Author"; count header ("N quotes" / "1
    quote"); FloatingActionButton (+, new ic_add) to add; tap a row to edit; swipe
    left/right (ItemTouchHelper) to delete with a confirm dialog. Add/edit share one
    dialog_quote_edit (TextInputLayouts: Quote + optional Author); blank quote text
    keeps the dialog open (positive button overridden in setOnShowListener)
  - QuoteManagementViewModel: quotes StateFlow off the new repository.observeQuotes();
    add/update (author trimmed, blank -> null) + delete with a LAST-QUOTE GUARD (pool
    must keep >=1 so the quote content screen always has something). deleteQuote reports
    success via callback so a blocked/cancelled swipe restores the row (notifyItemChanged);
    the fragment also blocks the swipe when size<=1, so it's guarded in two places
  - Data layer: BundledQuoteDao.observeAll() (Flow); repository observeQuotes /
    updateQuote / deleteQuote (only add/get existed before). nav_graph:
    quoteManagementFragment + action_settings_to_quotes
  - Stage 2 needed NO change: it already picks via getRandomQuote() = "ORDER BY RANDOM()
    LIMIT 1" over the whole table, so any custom quote is automatically eligible
  - VERIFIED on device (SM-A326B, adb/uiautomator-driven): list showed "40 quotes" with
    authors + FAB; added "Rise and grind, sleepyhead / Nick" -> "41 quotes", shown with
    author; edited a bundled quote (text changed, count stayed 41); swipe -> "Delete
    quote?" confirm -> delete -> "40 quotes", row gone. Step 4 (custom quote can appear
    in Stage 2) confirmed at the mechanism level: pulled the DB and ran Stage 2's exact
    query 400x — the custom quote was selected 6/400 (~1/41 as expected). A physical
    shake-through is still Nick's to confirm
  - Restored the one bundled quote deleted during testing (re-added Mark Twain); left the
    custom "Rise and grind, sleepyhead" in place so Nick can see it surface in Stage 2.
    Pool now 41 = 40 original bundled + 1 custom. Build + testDebugUnitTest green
  - NOT exercised live: the last-quote guard (would need deleting 40 quotes); enforced in
    code (VM count<=1 + fragment size<=1)

2026-06-17  Sound & alarm settings (sound pickers, volume, vibration, Stage 2 slider)
  - BUNDLED SOUNDS: 8 synthesized WAV tones in res/raw (16kHz mono, ~37-93KB each,
    ~630KB total) generated by a throwaway Python script — 4 gentle for Stage 1
    (Soft Chime, Gentle Bells, Warm Waves, Morning Glow) and 4 urgent for nuclear
    (Rapid Beep, Siren, Klaxon, Alarm Pulse). data/model/AlarmSounds registry maps a
    stored key -> displayName + R.raw id, with first-as-default fallback
  - DATA: AlarmConfig gained stage1SoundId, nuclearSoundId (keys), stage1Volume (0-100),
    vibrationEnabled. Migration v4->v5 (non-destructive ALTERs; types verified against
    the exported 5.json). SettingsViewModel exposes the config flow + setters routed
    through one persist() helper (also refactored the existing sequence/stage2 setters
    onto it)
  - SETTINGS UI: new "Alarm sounds & volume" section — two sound rows (current name +
    Preview button) that open a single-choice dialog previewing each sound on tap and
    saving on confirm; a Material volume Slider (persists on touch-release, not every
    tick); a "Vibrate during nuclear alarm" switch. util/SoundPreviewPlayer wraps a
    MediaPlayer for previews (stopped on dialog dismiss / onPause / onDestroyView)
  - VIBRATION TOGGLE controls the NUCLEAR alarm only — Stage 1 is intentionally
    sound-only (force-shake detection would read the motor as shaking). Labelled
    accordingly
  - STAGE 2 SLIDER (req 6): the existing PIN-gated duration row now opens a dialog with
    a Material Slider (5-15, live label) instead of a NumberPicker — keeps the PIN gate
    that the PIN task requires rather than regressing it
  - SERVICES: AlarmService and NuclearAlarmService now read the config off the main
    thread (IO coroutine) and play the chosen res/raw sound via openRawResourceFd +
    USAGE_ALARM instead of the system ringtone. AlarmService applies stage1Volume;
    NuclearAlarmService skips vibration when vibrationEnabled is false
  - VERIFIED on device (SM-A326B, adb/uiautomator): v4->v5 migration ran with no crash;
    section renders with defaults (Soft Chime/Rapid Beep, vol max, vibrate on). Changed
    wake-up->Warm Waves, nuclear->Siren, volume->31, vibration->off, Stage 2->15 (slider
    showed live "15 min"); all five persisted in alarm_config; force-stop + relaunch
    showed every choice retained. Build + testDebugUnitTest green
  - DEVICE NOTES: the phone had an unknown PIN set (likely by Nick) that blocked the
    PIN-gated Stage 2 row; cleared just shared_prefs/pma_security.xml (non-destructive,
    reversible) to test the slider — PIN is now OFF, re-set it in Settings if wanted.
    Restored volume=100 + vibration=on so the live alarm isn't left weakened; left
    sounds as Warm Waves/Siren and Stage 2 at 15 min. Audio output not heard over adb —
    preview/playback wiring confirmed (valid WAVs, no MediaPlayer errors), real loudness
    is Nick's to confirm

2026-06-17  Theme + battery-optimisation settings
  - THEME: util/ThemeManager (ThemeMode LIGHT/DARK/SYSTEM -> AppCompatDelegate night
    modes) persisted in shared prefs "pma_settings". Applied from
    PersonalMorningAlarmApp.onCreate (applySaved) so the right theme is set from app
    start; setThemeMode() calls setDefaultNightMode which recreates the live activity so
    the change shows immediately. Settings "Appearance" radio group (Light/Dark/Follow
    system); base theme is already Material3 DayNight so both modes render
  - BATTERY: util/BatteryOptimisationHelper — isIgnoringBatteryOptimizations
    (PowerManager), an explanation dialog, and requestExemption (ACTION_REQUEST_IGNORE_
    BATTERY_OPTIMIZATIONS with a package: uri, falling back to the settings list).
    Manifest gained REQUEST_IGNORE_BATTERY_OPTIMIZATIONS. One-shot launch prompt
    (shouldPromptOnLaunch / markPrompted, flag in pma_settings) shown from
    MainActivity.onCreate when not whitelisted; Settings "Battery" section shows status
    (optimised vs whitelisted, refreshed in onResume) + an "Allow background running"
    button that opens the same dialog
  - VERIFIED on device (SM-A326B, once Nick unlocked it): fresh launch showed the
    first-launch "Allow background running" dialog; Open settings -> the real
    RequestIgnoreBatteryOptimizations system prompt -> Allow whitelisted the app
    (deviceidle whitelist confirmed) and the Settings status flipped to "Whitelisted"
    with the button disabled. Theme: Light and Dark both applied IMMEDIATELY (activity
    recreated in place); Light survived a force-stop+relaunch despite the device being in
    dark mode, proving the saved choice is applied on app start. assembleDebug +
    testDebugUnitTest green
  - Left for Nick: theme back to Follow-system (default); app left WHITELISTED (better
    for real alarms); battery_prompted flag set so the one-shot launch prompt won't recur

2026-06-10  Stretch routine system (entities, management UI, Stage 2 integration)
  - DATA: two new entities — StretchRoutine (id, name, isActive, createdAt) and
    StretchExercise (id, routineId FK CASCADE, name, durationSeconds, instructions,
    displayOrder) + DAOs + repository methods. RoutineWithCount projection (routine +
    exercise count) for the list. AlarmConfig gained matchRoutineToGoal +
    exerciseRoutineId + projectRoutineId (per-goal mapping)
  - MIGRATION v3->v4: creates both tables (+ FK index) and adds the 3 alarm_config
    columns. To avoid hand-writing schema Room would reject, enabled exportSchema=true
    (ksp room.schemaLocation -> app/schemas) and copied the CREATE statements verbatim
    from 4.json. Non-destructive; the existing v3 DB upgraded cleanly on device
  - SEED (first launch, idempotent): 3 presets — Pre-Run Warmup (dynamic), General
    Morning Stretch (static, seeded active), Rest Day Mobility (gentle), 5 exercises each
  - UI: Settings -> "Manage stretch routines" -> StretchRoutinesFragment (list with
    active radios + "match to morning goal" switch + per-goal routine pickers + add FAB)
    -> tap a routine -> RoutineEditFragment (rename, add/edit stretch via 3-field dialog,
    delete stretch w/ confirm, reorder via up/down buttons, delete routine). Last-routine
    guard. routineId passed via nav argument
  - REQ 8 (active routine): manual via StretchRoutine.isActive (single active, set by the
    radio), OR matched to the morning goal (config flag + per-goal ids). repository.
    getStretchRoutineForGoal(goal) resolves: goal-mapped routine if matching is on, else
    the active one, else the first (so the screen always has content)
  - REQ 9 (Stage 2): AlarmDismissalActivity stretch screen now loads the active routine's
    exercises and steps through them one at a time — heading=name, body=instructions,
    new tv_stretch_progress="Stretch X of N", per-exercise countdown; auto-advances on
    each timer's finish, ends the block after the last (Continue still skips). Falls back
    to the old suggestions string if a routine has no exercises
  - VERIFIED on device (SM-A326B, adb/uiautomator-driven): migration ran with no crash;
    "Seeded 3 stretch routines"; list shows all 3 (General active). Tested: set active ->
    Pre-Run (DB isActive moved); reorder (move Shoulder up -> DB displayOrder swapped,
    confirmed visually); add stretch "Calf raises" (->6) then delete it w/ confirm (->5);
    created custom "Evening Wind-down" routine + renamed + added a stretch; enabled
    match-to-goal and mapped Exercise->Pre-Run, Project->General (config row updated).
    Stage 2 resolution verified by replicating getStretchRoutineForGoal against the DB:
    goal=PROJECT -> "General Morning Stretch" -> its 5 per-stretch screens w/ durations.
    Build + testDebugUnitTest green
  - Left configured for Nick: match-to-goal ON (Exercise->Pre-Run, Project->General, the
    PRD example), custom "Evening Wind-down" routine present, General reordered (Shoulder
    first from the reorder test). Live shake-through display of the stretch screen still
    for Nick to confirm. NOTE: pre-existing cruft — alarm_config has ~46 accumulated rows
    from earlier dev; getCurrent() uses the latest, so behaviour is correct

2026-06-21  Re-arm alarm on app launch (alarm silently stopped firing)
  - BUG (Nick hit it for real, 2nd time): after the 06-17 sessions the 7am alarm
    didn't fire on 06-18 or after, despite the toggle still showing ON. Diagnosed
    on-device via dumpsys alarm: re-arm chain worked daily 06-10..06-17 (last fire
    06-17 07:00 armed 06-18 07:00), but the 06-18 alarm was never delivered and was
    no longer pending — no alarm_cancelled log for its PendingIntent, i.e. it was
    dropped by a force-stop/stopped-state (the 06-17 dev sessions did force-stop +
    relaunch cycles), not an explicit cancel. With no fire, nothing re-armed 06-19+
  - ROOT CAUSE (structural): exact alarms are one-shot and only re-arm on fire
    (AlarmReceiver) or reboot (BootReceiver). A single dropped/missed fire severs the
    chain permanently, and the Home toggle reads isEnabled straight from the DB — so
    it shows ON while nothing is actually scheduled. No self-healing path on app open
  - FIX: MainActivity.rearmAlarmIfEnabled() — on every launch, off the main thread,
    reads config and (if enabled + exact-alarm permission held) calls
    scheduleDailyAlarm(). Idempotent (FLAG_UPDATE_CURRENT overwrites). Now simply
    opening the app guarantees the next occurrence is armed, closing the force-stop /
    missed-fire hole. Joins the existing fire-time + boot re-arm paths
  - VERIFIED on device (SM-A326B): installDebug; launched -> logcat "Re-armed alarm
    on launch (alarmTime=420, ok=true)"; dumpsys alarm now shows a live pending
    RTC_WAKEUP for 2026-06-22 07:00:00 (exactAllowReason=policy_permission). Device
    left armed for tomorrow 07:00, so Nick is covered regardless. assembleDebug clean

2026-06-22  Volume overhaul (alarm too quiet, restore old sound, nuclear unmutable)
  - WHY: Nick's alarm fired but was too quiet to wake him. The 06-17 work had
    replaced the original system alarm tone with 5 synthesized WAVs and made volume
    a MediaPlayer.setVolume scale WITHIN the device alarm stream — so a low device
    alarm slider made the app quiet regardless of the in-app setting, and the preview
    ignored volume entirely (min/max sounded identical)
  - NEW VOLUME MODEL (Stage 1): AlarmService now SETS the device alarm stream
    (STREAM_ALARM) to the app-set volume % of max on every fire (applyAlarmStreamVolume),
    and plays the MediaPlayer at full. So the in-app slider is the real loudness and the
    alarm starts at the configured level each morning no matter where the slider was
    left. AlarmDismissalActivity sets volumeControlStream = STREAM_ALARM, so the hardware
    buttons turn the alarm down WHILE shaking; the next fire resets it. The in-app slider
    (not the device slider) is now the quiet-testing safeguard
  - OLD SOUND BACK: added "Default (system alarm)" as the first Stage 1 sound option
    (AlarmSound.resId==0 -> resolves to RingtoneManager TYPE_ALARM default at playback).
    It's now AlarmSounds.defaultStage1, so blank config picks it. AlarmSounds.setDataSource
    centralises raw-res-vs-system-default playback for the services + preview. NOTE: Nick's
    saved choice is still the 06-17 pick (gentle_warm_waves) — he selects "Default (system
    alarm)" from the picker to get the original tone
  - PREVIEW REFLECTS VOLUME: SoundPreviewPlayer.play now takes a volume (0f-1f) and
    setVolume()s it. Settings passes the live slider value for Stage 1 previews (row +
    picker), 1f for nuclear. Fixes the min==max preview bug
  - NUCLEAR UNMUTABLE: NuclearAlarmService FORCE_MAX_VOLUME flipped false->true (forces
    STREAM_ALARM to max on start); NuclearDismissalActivity now swallows VOLUME_UP/DOWN/
    MUTE in onKeyDown AND onKeyUp (isBlockedKey), so the hardware buttons can't quieten or
    mute it. The in-app slider does NOT affect nuclear (always max). TESTING NOTE: nuclear
    is now genuinely loud — flip FORCE_MAX_VOLUME false temporarily to test quietly
  - VERIFIED on device (SM-A326B): assembleDebug + testDebugUnitTest green; installDebug.
    Fired the test alarm with the device alarm stream pre-set to 11/15 -> logcat
    "Set alarm stream to 15/15 (100%)" (app vol is 100), proving the fire raises/resets
    the stream. While Stage 1 was sounding, 4x VOLUME_DOWN keyevents dropped STREAM_ALARM
    to 1 (turn-down-while-shaking confirmed routes to the alarm stream). force-stop to
    silence (can't shake over adb) cancelled the pending alarm; relaunch re-armed 07:00
    ("Re-armed alarm on launch"). NOT verified over adb (no audio capture / would blast
    the house): actual loudness of system-default tone, preview tracking the slider by
    ear, and the nuclear force-max + volume-key block live — Nick to confirm by ear.
    Picker entry is static compiled code (first in AlarmSounds.stage1) so guaranteed present

2026-06-22  Preview fix: preview rides the alarm stream, not media
  - Nick's catch: with app volume max but DEVICE (media) volume at zero, the preview
    was silent — and he worried that meant the real alarm would be silenced by turning
    the phone down in another app. It would NOT: the volume buttons in other apps
    control the media/ring stream, while the alarm plays on STREAM_ALARM, which
    AlarmService force-sets to the app volume on every fire (so even a zeroed alarm
    stream is reset before playback). The bug was only the PREVIEW being misleading
  - ROOT CAUSE: SoundPreviewPlayer used a bare MediaPlayer (default = media stream), so
    media volume governed it. The real alarm uses USAGE_ALARM
  - FIX: preview now plays on USAGE_ALARM and SETS STREAM_ALARM to the chosen volume %
    for the duration (exactly what the real alarm does), then restores the prior alarm
    level when the preview ends (saved/restored in play()/stop(); completion routes
    through stop()). So the preview is a true rehearsal, unaffected by media/ring volume.
    Settings already passes the right fraction (live slider for Stage 1, 1f for nuclear)
  - assembleDebug clean; installed on SM-A326B; relaunch re-armed 07:00. Audible
    behaviour (media-zero no longer silences preview; preview loudness matches the alarm)
    is Nick's to confirm by ear — adb can't capture audio

2026-07-05  Unit test suite (56 tests across challenges, repository, ViewModels)
  - Grew the test module from 2 files to 8; 56 tests, all green via ./gradlew test
    (runs both testDebugUnitTest + testReleaseUnitTest)
  - NEW TEST DEPS (app/build.gradle): Robolectric 4.13 + androidx.test:core + room-
    testing + kotlinx-coroutines-test 1.7.3 (matches the transitive coroutines
    version). testOptions.unitTests.includeAndroidResources=true so Robolectric can
    run Room/framework code on the local JVM. src/test/resources/robolectric.properties
    pins sdk=33 (compileSdk 35 isn't supported by Robolectric 4.13; 33 = the A32 test
    device anyway)
  - MathChallengeTest (pure JVM, 7): answer validation (whitespace/wrong/non-numeric,
    never crashes), deterministic generation via seeded Random, operands 10..99,
    subtraction never negative, same-seed reproducibility
  - ShakeChallengeTest (pure JVM, 10): threshold detection, cumulative duration
    tracking (pause-tolerance bridging, interval cap), progress 0..1 + clamp,
    rebaseline-preserves-progress, reset. REFACTOR TO ENABLE THIS: extracted the
    per-sample timing/threshold math out of ShakeChallenge into a new pure
    ShakeAccumulator (injectable clock — the live class fed System.currentTimeMillis
    so duration couldn't be driven deterministically). ShakeChallenge now delegates;
    the "no shake / no baseline yet" sentinels became explicit booleans (identical
    behaviour under epoch-scale time, but robust at any test time base). On-device
    shake behaviour is unchanged — same arithmetic, same constants
  - RepositoryTest (Robolectric + in-memory Room, 20): CRUD for all 7 entities
    (config/event/tag/toggle/quote/routine/exercise incl. FK cascade + single-active
    routine + goal-mapping resolution) and streak/weekly edge cases: no data, single
    day today, success-yesterday-still-live, dead streak (>1 day old), consecutive,
    gap breaks at gap, nuclear-today breaks immediately, duplicate-same-day, longest
    historical run, weekly counts/rate windows
  - HomeViewModelTest + SettingsViewModelTest (Robolectric + coroutines-test, 4+5):
    StateFlow state management/emissions over a real in-memory-Room repo. Harness
    (ViewModelTestSupport) runs viewModelScope on a real single-thread Main dispatcher,
    keeps the WhileSubscribed flows hot with a collector, and awaits state on the wall
    clock (Room emits on its own executor — avoids virtual-clock races). Covers config
    tracking, create-disabled-default, single-row in-place edits, stats streak/weekly,
    content toggles, sequence/stage2/active-tag flows, sound/volume/vibration setters.
    NOTE: firing multiple persist() setters without awaiting between them races (each
    does read-modify-write on the one row, last write wins); the UI only changes one
    at a time, so tests sequence the awaits to match
  - Existing NfcCheckpointManagerTest/ContentScreenManagerTest kept; added an explicit
    order-randomisation assertion to the NFC test
